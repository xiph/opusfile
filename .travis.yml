language: c

compiler:
  - gcc
  - clang

os:
  - linux
  - osx

addons:
  apt:
    packages:
      - libogg-dev
      - libopus-dev

install: if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew update && brew bundle; fi

script:
  - ./autogen.sh
  - ./configure
  - make
  - make distcheck
